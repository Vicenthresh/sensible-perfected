# Mod jars (tracked via references, not binary files)
*.jar
*.jar.disabled

# Exports (large binary archives — attach to GitHub releases instead)
exports/*.mrpack

# Version compatibility reports (generated, not tracked)
reports/

# Runtime / personal
saves/
logs/
crash-reports/
*.mv.db
*.trace.db
usercache.json
options.txt
